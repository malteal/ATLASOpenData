
hydra:

  # listmerge: append

  job:
    chdir: True

  run:
    dir: ${paths.save_path}

defaults:
  - _self_
  - paths: default.yaml
  - variables: default.yaml
  - root: default.yaml

tree: CollectionTree;1
style: FTAG # event